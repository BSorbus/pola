<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attachmenable_id %>
    <%= form.text_field :attachmenable_id, id: :attachment_attachmenable_id %>
  </div>

  <div class="field">
    <%= form.label :attached_file %>
    <%= form.text_field :attached_file, id: :attachment_attached_file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
